# Pick one or more — only the one matching your selected model is needed

# Claude (console.anthropic.com)
CLAUDE_API_KEY=sk-ant-...

# Groq — fast, free tier (console.groq.com)
GROQ_API_KEY=gsk_...

# Ollama — local, no key needed, set OLLAMA_HOST=0.0.0.0 on Windows first
OLLAMA_API_BASE=http://<windows-host-ip>:11434
