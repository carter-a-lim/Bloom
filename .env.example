# Required: Claude API key — used by atomizer, worker executor, and aggregator
CLAUDE_API_KEY=sk-ant-...

# Optional: Galactic CLI config path (WSL users only)
GALACTIC_CONFIG_PATH=/path/to/galactic/config
